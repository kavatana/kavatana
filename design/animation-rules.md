# Animation Rules

- All transitions max 200ms.
- Only hover states, focus states, and form feedback are animated.
- Implement `prefers-reduced-motion`: all transitions set to 0ms when this media query is active.
- No scroll-triggered animations.
- No entrance animations on page load.
- No decorative motion.
